#include<iostream>
#include<vector>
#include<string>
using namespace std;

bool is_pre(const vector<int>&s,const vector<int>&m)
{
    if(s.size()>m.size())
        {return is_pre(m,s);}
          for(int i=0;i<s.size();++i)
               {
                   if(s[i]==m[i])
                          return true;
                      else     return false;
                }
           
}
int main()
{
    vector<int> s{0,1,1,2};
    vector<int> m{0,1,1,2,3,5,8};
    cout<<(is_pre(s,m)?"yes":"no");
    return 0;

}
